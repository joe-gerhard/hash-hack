import styled, { css } from 'styled-components';
import { Theme } from '../../styles/theme';

type SearchPageProps = {
    theme: Theme;
}

const SearchPage = styled('div')(({ theme }: SearchPageProps) => css`
    
`);

export default {
    SearchPage,
}