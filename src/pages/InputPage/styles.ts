import styled, { css } from 'styled-components';
import { Theme } from '../../styles/theme';

type InputPageProps = {
    theme: Theme;
}

const InputPage = styled('div')(({ theme }: InputPageProps) => css`
    
`);

export default {
    InputPage,
}